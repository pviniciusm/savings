import { CreateIncomeUseCase } from './createIncome';

describe('create income use case tests', () => {
    test('should return anything in order to check if jest is installed', () => {
        expect(1).toBe(2 - 1);
    });
});
